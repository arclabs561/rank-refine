--- src/matryoshka.rs
+++ replace >= with < in try_refine
@@ -109,17 +109,17 @@
 /// Returns [`RefineError::InvalidHeadDims`] if `head_dims >= query.len()`.
 pub fn try_refine<I: Clone + Eq + Hash>(
     candidates: &[(I, f32)],
     query: &[f32],
     docs: &[(I, Vec<f32>)],
     head_dims: usize,
     config: RefineConfig,
 ) -> Result<Vec<(I, f32)>> {
-    if head_dims >= query.len() {
+    if head_dims < /* ~ changed by cargo-mutants ~ */ query.len() {
         return Err(RefineError::InvalidHeadDims {
             head_dims,
             query_len: query.len(),
         });
     }
 
     let doc_map: HashMap<&I, &[f32]> = docs
         .iter()
