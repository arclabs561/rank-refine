--- src/scoring.rs
+++ replace - with / in normalize_scores
@@ -167,17 +167,17 @@
         });
 
     let range = max - min;
     if range < 1e-9 {
         // All scores equal, return 0.5 for all
         return vec![0.5; scores.len()];
     }
 
-    scores.iter().map(|&s| (s - min) / range).collect()
+    scores.iter().map(|&s| (s / /* ~ changed by cargo-mutants ~ */ min) / range).collect()
 }
 
 // ─────────────────────────────────────────────────────────────────────────────
 // Pooler Trait
 // ─────────────────────────────────────────────────────────────────────────────
 
 /// Token embedding compression.
 ///
