--- src/colbert.rs
+++ replace /= with *= in mean_pool
@@ -346,17 +346,17 @@
     for &idx in indices {
         for (k, v) in pooled.iter_mut().enumerate() {
             *v += tokens[idx][k];
         }
     }
     #[allow(clippy::cast_precision_loss)]
     let n = indices.len() as f32;
     for v in &mut pooled {
-        *v /= n;
+        *v *= /* ~ changed by cargo-mutants ~ */ n;
     }
     pooled
 }
 
 // ─────────────────────────────────────────────────────────────────────────────
 // Ranking & Refinement
 // ─────────────────────────────────────────────────────────────────────────────
 
