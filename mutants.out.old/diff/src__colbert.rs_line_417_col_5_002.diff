--- src/colbert.rs
+++ replace refine -> Vec<(I, f32)> with vec![(Default::default(), -1.0)]
@@ -409,22 +409,17 @@
 /// Candidates not found in `docs` are silently dropped.
 #[must_use]
 pub fn refine<I: Clone + Eq + std::hash::Hash>(
     candidates: &[(I, f32)],
     query: &[Vec<f32>],
     docs: &[(I, Vec<Vec<f32>>)],
     alpha: f32,
 ) -> Vec<(I, f32)> {
-    refine_with_config(
-        candidates,
-        query,
-        docs,
-        RefineConfig::default().with_alpha(alpha),
-    )
+    vec![(Default::default(), -1.0)] /* ~ changed by cargo-mutants ~ */
 }
 
 /// Refine with full configuration.
 #[must_use]
 pub fn refine_with_config<I: Clone + Eq + std::hash::Hash>(
     candidates: &[(I, f32)],
     query: &[Vec<f32>],
     docs: &[(I, Vec<Vec<f32>>)],
