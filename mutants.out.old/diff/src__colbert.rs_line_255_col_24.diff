--- src/colbert.rs
+++ replace += with *= in pool_tokens_sequential
@@ -247,17 +247,17 @@
 
     tokens
         .chunks(window_size)
         .map(|chunk| {
             let dim = chunk[0].len();
             let mut pooled = vec![0.0; dim];
             for token in chunk {
                 for (k, v) in pooled.iter_mut().enumerate() {
-                    *v += token[k];
+                    *v *= /* ~ changed by cargo-mutants ~ */ token[k];
                 }
             }
             #[allow(clippy::cast_precision_loss)]
             let n = chunk.len() as f32;
             for v in &mut pooled {
                 *v /= n;
             }
             pooled
