[package]
name = "rank-refine-evals"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rank-refine = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

