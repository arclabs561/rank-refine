[package]
name = "rank-refine"
version = "0.7.5"
edition = "2021"
rust-version = "1.74"
license = "MIT OR Apache-2.0"
description = "Fast reranking — Matryoshka, ColBERT MaxSim, Cross-encoder (SIMD-accelerated)"
keywords = ["search", "rerank", "colbert", "matryoshka", "simd"]
categories = ["algorithms", "science"]
repository = "https://github.com/arclabs561/rank-refine"
readme = "README.md"
authors = ["Arc <attobop@gmail.com>"]

[dependencies]
kodama = { version = "0.3", optional = true }

[dev-dependencies]
criterion = "0.5"
proptest = "1"

[features]
default = []
hierarchical = ["kodama"]

[[bench]]
name = "refine"
harness = false
